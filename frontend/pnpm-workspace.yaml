ignoredBuiltDependencies:
  - sharp
  - unrs-resolver

# Security floor (2026-09-23): monaco-editor pins dompurify exactly (3.2.7; even 0.56.0 pins 3.4.8),
# below the fixed 3.4.9, so `pnpm update` cannot reach it. Drop this once monaco-editor ships >= 3.4.9.
overrides:
  dompurify: ^3.4.9
