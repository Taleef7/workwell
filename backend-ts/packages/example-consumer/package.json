{
  "name": "@workwell/example-consumer",
  "version": "0.1.0",
  "description": "Proves @workwell/measure-engine evaluates content that is not WorkWell's: one dependency, its own measure, its own ELM, its own bundle. Not published — it exists to be run in CI (roadmap M-C / C2, ADR-062).",
  "type": "module",
  "license": "Apache-2.0",
  "private": true,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "dependencies": {
    "@workwell/measure-engine": "workspace:*"
  },
  "files": [
    "src"
  ]
}
