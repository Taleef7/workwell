# Reuse task outputs across CI runs when inputs match.
org.gradle.caching=true

# Reuse configuration work between repeated CI runs.
org.gradle.configuration-cache=true

# Let Gradle overlap independent work when the runner has spare cores.
org.gradle.parallel=true
