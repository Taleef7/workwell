spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/workwell}
  flyway:
    enabled: true
    url: ${DATABASE_URL_DIRECT:${spring.datasource.url}}
management:
  endpoints:
    web:
      exposure:
        include: health
