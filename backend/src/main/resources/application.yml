spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/workwell
    username: workwell
    password: workwell
  flyway:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: health
