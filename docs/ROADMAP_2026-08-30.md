# WorkWell Strategic Roadmap — the engine meets its first customer (2026-08-30)

**Status:** APPROVED (owner, 2026-08-30). Supersedes `docs/ROADMAP_2026-08-04.md` as the active direction.
That document's verification model (§4, the FHIR-column set) and its owner decisions 1–4 **stand unchanged**;
what changes is the **spearhead**, the **consumer of the engine**, and the sequencing of M-E.

**Sources:** the 2026-08-27 working session with MIE leadership and the pilot customer's quality team; the
pilot's proposed PY2027 quality measure set (reviewed by the owner; source materials are local-only and not
in this repository); the CY2027 PFS proposed rule (CMS-1848-P, July 2026); and the owner conversation of
2026-08-30. Driving ADR: **ADR-070**.

> **Naming policy (owner, 2026-08-30):** repository documents refer to the pilot deployment as **"Maui"**
> (an instance name, following the TWH pattern) and to the customer as **"the pilot group."** No client
> legal names, no client staff names, no reproduction of client-provided documents. Details live in
> local-only working notes.

---

## 1. What changed, and why this supersedes 2026-08-04

### 1.1 The engine has a customer, and the customer's demand shape is already built

A primary-care group on MIE's WebChart (the pilot group) is entering a **Medicare Shared Savings Program
(MSSP) ACO** with performance year **PY2027 — measurement begins 2027-01-01**. The ACO's proposed quality
set is the **APP Plus** measure set (APM Performance Pathway Plus — the fixed, phased-in set MSSP ACOs
report; it is a mandate, not a menu). Its EMR-reported clinical half, decoded from MIPS quality IDs to the
eCQMs they correspond to:

| MIPS ID | Measure | eCQM | WorkWell status (2026-08-30) |
|---|---|---|---|
| 001 | Diabetes/Glycemic Status >9% | CMS122 | Vendored, MADiE-gated 55/55, **routed official on TWH** |
| 134 | Depression Screening & Follow-Up | CMS2 | Vendored, gated 36/36, routable |
| 236 | Controlling High Blood Pressure | CMS165 | Vendored, gated 68/68, routable |
| 112 | Breast Cancer Screening | CMS125 | Vendored, gated 66/66, **routed official on TWH** |
| 113 | Colorectal Cancer Screening | CMS130 | Vendored, gated 64/64, routable |
| 305 | Initiation & Engagement of SUD Treatment | CMS137 | **Not vendored — the one gap** |

The set's other three members are **not EMR-computed**: two claims-calculated outcome measures
(hospital-wide readmission; admissions for multiple chronic conditions) and CAHPS for MIPS (survey vendor).
No engine work exists for them; at most the Maui dashboard shows them as informational tiles so the quality
team sees the whole set in one place.

**Five of the six computable measures are already vendored, MADiE-gated, and two run CMS's official
artifacts in production.** The 2026-07-24 priority-measure bet (Nicole's list) turns out to have been a bet
on exactly the measures an MSSP ACO demands. That is the position this roadmap spends.

### 1.2 What the customer meeting changed about the product

Three findings from the 2026-08-27 session, each a direction rather than a feature request:

1. **The consumer of the engine is a clinical quality team working provider panels, not another EMR.**
   The quality staff divide gap-chasing work **by provider panel** (each staffer owns specific PCPs'
   patients), explicitly not by measure — one patient may fail five measures and exactly one person should
   own the patient. Everything the roster/case UI grows next follows from that: patient-centric work
   lists, PCP/location filters, saved per-staff filters, and eventually attribution.
2. **A care-gap card must offer resolution, not acknowledgment.** Accept/dismiss is rejected. The right
   actions are (a) *place the order that closes the gap* — which for simple measures (an A1C) is a standing
   order and for complex ones (colorectal: colonoscopy vs stool-DNA vs FIT; breast: laterality/implant
   variants) is a pick list requiring clinician discretion — and (b) *document why the patient should leave
   the denominator* (exclusion/exception), so the next CQL run excludes them. Both fit ADR-067's existing
   gating (suggestions only on APPROVED terminology mappings; no `systemActions`, no `critical`) — the new
   work is populating mappings and building the exception-documentation path, not changing the safety rules.
3. **Orders are inherently local, and that is the integration's hard problem.** A practice's order catalog
   is thousands of locally-named rows (often with billing codes but no LOINC); imaging centers are not
   required to return LOINC on results. MIE's direction: put LOINC/SNOMED on the order rows so measure
   pick lists derive from value-set membership automatically, and let a returning result inherit the LOINC
   from its requisition's order code. That mapping work is MIE-side; WorkWell consumes it. Written
   documentation of the mechanism is a named external dependency (§7).

Two smaller but immediate product changes: the pilot instance is **patient-driven** ("patient," not
"employee," as deployment-level terminology), and the dashboard's status-count chips must **deep-link to
the pre-filtered list they count** — the quality team clicks a number and lands on the patients to work,
never re-filtering.

### 1.3 What is demoted, and what this does NOT change

- **The versioned compliance API loses its "primary contract" framing** (2026-08-04 decision 5). The
  standalone multi-EMR API consumer is not the near-term integration story; the card/CDS surface and the
  Maui instance are. **The API itself stays** — versioned, documented, tested, serving the CDS service and
  any future consumer — but no work is justified by "the API contract needs it" any more than by
  "certification needs it." `COMPLIANCE_API.md` remains accurate; only its strategic weight changes.
- **M-E1 (occupational content pack) defers.** Locked decision 6 — occupational measures as the long-term
  differentiator — **stands**; the pilot simply proves the engine on a paying-customer-shaped problem
  first. Nothing occupational is deleted.
- **ADR-058's decisions 1–4 stand untouched:** no ONC certification pursuit, the FHIR-column verification
  set stays the bar, no QDM engine and no relabelling, QRDA I/III kept as the interoperability bridge.
- **The packages stand:** `@work-well/measure-engine` / `measure-codegen` remain published; the Maui work
  consumes the same injected-content engine every other surface does.

---

## 2. Owner decisions (locked 2026-08-30)

Recorded in `docs/LOCKED_DECISIONS.md` §4A; summarized here:

1. **The Maui pilot is the spearhead** — milestone M-M below supersedes M-E1 as next-up. M-E1 is deferred,
   not cancelled.
2. **The pilot's measure catalog is the APP Plus computable set**: CMS122, CMS2, CMS165, CMS125, CMS130
   (existing) plus **CMS137** (to vendor — spike first, see §4). The MIPS↔CMS crosswalk becomes a
   first-class UI surface, because the quality team thinks in MIPS numbers.
3. **Cards resolve, not alert**: order suggestions and exception documentation are the two card actions.
   ADR-067's refusals (`critical`, `systemActions`, approved-mapping gating, cards render completed
   evaluations) are unchanged and load-bearing.
4. **The compliance API is demoted from "the contract MIE consumes" to "a served surface"** — kept,
   versioned, not the spearhead's justification.
5. **Cheap-first sequencing**: MM-0 (instance + UX wins) ships before the externally-blocked work (order
   pick lists, real-time ingest, new-UI embedding).
6. **Naming policy**: deployment name "Maui" only in repo docs; client identities and source materials stay
   local-only.

---

## 3. North star (revised wording, same star)

**The engine is still the product.** What changes is the proof: through 2026-08, the proof was
measurement against external rulers (MADiE, HL7 validators, cross-engine execution). From 2026-09, the
proof is **a real quality team running its ACO year on the engine** — same artifacts, same gates, plus a
user who will find what no harness can. TWH remains the occupational-health showcase; Maui becomes the
primary-care/ACO one. Both are deployments of one codebase.

---

## 4. The CMS137 gap — eyes open

CMS137 ("Initiation and Engagement of Substance Use Disorder Treatment") is the only computable APP Plus
measure not in the tree, and it is not "vendor a seventh measure":

- **It is a multi-rate measure** — two numerators (initiation ≤14 days; engagement within 34 days of
  initiation) over related populations. Everything routed so far is single-rate proportion. The router,
  MeasureReport/QRDA builders, outcome evidence shape, and the roster's one-status-per-subject rule all
  assume one rate. **A spike precedes any promise** (MM-1a): confirm the artifact + MADiE cases exist in
  `cqframework/dqm-content-qicore-2025`, run it through the ADR-047 gate harness, and inventory exactly
  which surfaces break on population-set multiplicity.
- **ADR-047's rule applies unchanged**: no measure routes without a green MADiE gate. Likely capped
  expansions → the credentialed VSAC vendor workflow (the ADR-054 path).
- **No authored counterpart exists**, so `flip-snapshot`'s authored-vs-official comparison cannot run —
  the known wave-2 unroutability wrinkle. The Maui pilot forces the general answer: a flip gate for
  official-only measures built from the MADiE gate + the tenant-data initial-population check
  (`DEPLOY.md` flip checklist), without an authored oracle.
- Two existing debts become customer-facing and rise accordingly: **CMS2's 7 unexplained `NUMER 1→0`
  mismatches** (depression screening is a pilot measure) and **CMS130/CMS165 being unswept**.

---

## 5. Milestones (M-M, ordered; each lists its verification gate)

### MM-0 — the Maui instance + the cheap wins *(first; target: demo-able at the pilot's work-list review)*

The pilot group's team gets a sandbox to beat on. Scope:

- **Second deployment** of the same codebase (MIE Create-a-Container + Neon, the TWH pattern): own
  container, own database/schema, own env. Provisioning the container and DNS is an **owner/MIE step**.
- **Deployment-level terminology**: "patient" replaces "employee" across the UI via configuration
  (extending the ADR-004 runtime brand switcher's pattern), never a fork. TWH keeps "employee."
- **Clickable status chips**: every dashboard count (compliant / due soon / overdue / missing data —
  global and per-measure) deep-links to the pre-filtered roster or case list it counts.
- **Sandbox accounts** for the pilot's named users — hardcoded accounts per the standing auth rule
  (acceptable for a sandbox; a production Maui tenancy triggers the `PRODUCTION_READINESS` auth fork and
  is out of scope here).
- **Primary-care-shaped synthetic roster**: extend the corpus generator so the five live measures each
  have subjects in every outcome bucket (hypertension, depression-screening, CRC-age, mammography, and
  diabetes cohorts). Synthetic only; no client data.
- **MIPS↔CMS crosswalk** surfaced wherever a measure is named (catalog, roster, case detail): "MIPS 112 ·
  CMS125 · Breast Cancer Screening."

*Verification:* frontend lint+build green; deep-link tests (chip → filtered list, filter state pinned);
terminology-config test (no "employee" string renders under the patient-mode config); backend tests for
any config surface added; the standard idempotency/audit invariants on anything touched.

### MM-1 — the measure set

- **MM-1a (spike):** CMS137 feasibility as §4 describes — outcome is a written finding (multi-rate surface
  inventory + gate result), not a routing.
- **MM-1b:** vendor CMS137, extend the MADiE CI gate (`OFFICIAL_GATED_MEASURES` grows by one; the gate
  count grows by its case count), implement multi-rate support where the spike found it missing.
- **MM-1c:** flip the pilot's five existing measures to official routing on the Maui deploy workflow —
  same reviewed-workflow-edit mechanism as PR-9c/ADR-045, same `official-flip-config.test.ts` guard
  (which already validates *any* workflow-shipped flip list), same pre-flip checklist against Maui's own
  data. Gray-measure informational tiles land here too.
- **MM-1d:** run down CMS2's 7 mismatches; sweep CMS130/CMS165 (the credentialed-vendor follow-through).

*Verification:* the MADiE gate stays exact (0 unexpected, 0 errors) at its new size; flip-config
structural + sidecar halves cover the Maui workflow file; `flip-snapshot` (or its official-only successor
from §4) run against the Maui roster before any flip.

### MM-2 — work lists & assignment (the provider-panel model)

- **PCP attribution field** on the subject directory (source: WebChart's PCP data where present; the
  synthetic roster gets one generated). Any schema change is **owner-written** per the standing rule.
- **Filters**: roster/case lists filterable by PCP and location; **saved filters** per staff account;
  patient-centric view (one patient, all open gaps) as the primary work-list unit.
- **Assignment**: cases assignable to staff; panel-based bulk assignment ("assign Dr. X's panel to staff
  Y"). Attribution *semantics* (taxonomy-based ACO attribution, PCP-outside-the-practice handling) are
  deliberately deferred until the ACO answers the open questions in §7 — the data model must not encode a
  guess.

*Verification:* filter/assignment tests including the empty-panel and reassignment paths; audit events on
every assignment mutation; saved-filter round-trip tests.

### MM-3 — resolution actions on cards *(design now; wiring blocked on externals)*

- **Order pick lists**: a card for an open gap lists the orders that resolve it, derived from the
  order-mapping surface MIE is building (order rows carrying LOINC/SNOMED mapped to measure value sets).
  Until that surface exists, the ADR-067 gate stands as-is: link-only cards for measures without approved
  mappings. **Blocked on** MIE's order-mapping documentation.
- **Exception documentation**: the "dismiss" path becomes "remove from denominator, with a documented
  reason." Hard constraint carried from AI_GUARDRAILS/ADR-008: WorkWell never overrides CQL — the action
  must produce **structured data the measure logic reads on the next run** (an exclusion-qualifying
  procedure/condition/refusal documented in the chart, or a WorkWell waiver where the authored logic reads
  waivers), not a WorkWell-side status flip. Which exception forms the pilot's clinicians actually use is
  a Nicole/clinical question — **blocked on** that consultation for specifics; the data-flow design is not.

*Verification:* mutation-checked card-action tests in the ADR-067 style (an unapproved mapping renders no
order; an exception writes its structured form + audit event and never touches `outcomes`).

### MM-4 — encounter-time integration *(blocked on MIE's new-UI source access)*

- **Real-time ingest**: WebChart pushes (or WorkWell subscribes to) data changes during an encounter so
  cards reflect the visit in progress. Design constraint carried from ADR-067, restated because the
  customer ask ("cards update live in the encounter") superficially conflicts with it: **a card remains a
  rendering of a completed evaluation** — freshness comes from evaluating *sooner* (event-driven
  evaluation on ingest), never from a card invocation triggering evaluation. If that line cannot hold at
  acceptable latency, ADR-067 is revisited explicitly — not eroded.
- **Card embedding in MIE's new UI**: WorkWell cards inside the new WebChart chart experience, via the
  CDS-service surface. Explicit standing instruction from MIE: integration work happens against the
  environment MIE provides, **not** against a live WebChart instance.
- Performance constraint from the customer, adopted as a requirement: the encounter must not get slower —
  evaluation stays in WorkWell's backend, asynchronous to the clinician's page.

*Verification:* the existing CDS service tests extend to the ingest-evaluate-render path; latency budget
asserted; the #470 per-measure-query fix lands with (or before) this milestone since CDS invocation volume
rises.

---

## 6. Verification model

**Unchanged.** `docs/ROADMAP_2026-08-04.md` §4 — the FHIR-column verification set — remains the bar
(locked decision 2 of 2026-08-04). The Maui work adds customer-facing gates on top, per milestone above;
it removes none. The MADiE gate remains the routing precondition for every measure including CMS137.

---

## 7. External dependencies and open questions (named, so silence is visible)

| # | Item | Owed by | Blocks |
|---|---|---|---|
| 1 | Order-mapping mechanism documentation (LOINC on order rows; result inherits order's LOINC) | MIE | MM-3 wiring |
| 2 | New-UI source/environment access for card embedding | MIE | MM-4 |
| 3 | Maui container provisioning + DNS | MIE / owner | MM-0 deploy |
| 4 | Exception/exclusion documentation guidance (which forms clinicians use) | Nicole consultation | MM-3 specifics |
| 5 | ACO answers: attribution basis (taxonomy?), reporting mechanism (eCQM vs Medicare CQM vs MIPS CQM), reporting cadence | ACO working session | MM-2 semantics; reporting scope |
| 6 | CY2027 **final** rule (~Nov 2026) — reconfirm APP Plus mechanics; same CMS-1848-P as the standing FHIR-timeline correction | CMS | Reporting-mechanics claims |
| 7 | Pilot measure-set confirmation after the ACO finalizes (the reviewed set is *proposed*) | Pilot group / ACO | MM-1 completeness |

None of these blocks MM-0.

---

## 8. Risks

- **CMS137 multi-rate support is the largest unknown** — sized only after the MM-1a spike; the honest
  failure mode is "larger than a milestone," in which case the pilot launches on five measures and says so.
- **PY2027 timing**: the pilot's year starts 2027-01-01; the sandbox must earn trust months earlier.
  Cheap-first sequencing is the mitigation — visible progress while blocked items wait.
- **External-dependency stall** (§7 items 1–2 have no dates). Mitigation: MM-0→MM-2 are fully unblocked;
  the roadmap never queues behind a dependency it can route around.
- **Scope gravity from a live customer**: a real user generates requests faster than one developer ships.
  Mitigation: the locked milestone order plus the standing no-silent-scope-change rule; new asks land in
  the backlog, not the current milestone.
- **Naming/privacy drift**: client details leaking into a public repo. Mitigation: the §1 naming policy is
  locked, and source materials live under a gitignored path.

---

## 9. Open owner steps

1. Confirm the Maui container/DNS provisioning path with MIE (§7.3).
2. Obtain the order-mapping documentation and new-UI access when offered (§7.1–2).
3. Schedule the Nicole consultation on exceptions (§7.4).
4. Carry the standing steps forward unchanged: npm Trusted Publishing migration; the Doug/Nicole
   confirmation that engine certification is not a business goal (ADR-058's one reopen condition).
5. After the CY2027 final rule (~Nov 2026): re-verify §1.1's measure table and the reporting-mechanism
   assumption; update `STANDARDS_CONFORMANCE.md` if mechanics moved.

---

## 10. Reference links (public sources only)

- QPP measure specifications (MIPS quality IDs → specifications): https://qpp.cms.gov/mips/explore-measures
- eCQI resource center (eCQM specifications, incl. CMS137): https://ecqi.healthit.gov/ep-ec
- Medicare Shared Savings Program (MSSP) + the APP Plus quality measure set (established in the CY2025 PFS
  final rule, phased in through PY2028): https://www.cms.gov/medicare/payment/fee-for-service-providers/shared-savings-program-ssp-acos
- CY2027 PFS proposed rule (CMS-1848-P, July 2026) — the same rule tracked by the standing FHIR-reporting-timeline correction
- `cqframework/dqm-content-qicore-2025` — the official QI-Core artifact + MADiE test-case source (ADR-047)
