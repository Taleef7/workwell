# Doug Wave — Implementation Plan (stacked PR wave)

**Date:** 2026-07-20 · **Spec:** `docs/superpowers/specs/2026-07-20-doug-wave-wcdb-shim-cql-sql-design.md`
· **Deadline:** demo-ready Wed 2026-07-22 EOD (Thursday 2026-07-23 call; Wed PM = buffer).

Preflight (done 2026-07-20): `wcdb` container boots; 56 patients; LOINC coverage counted
(BMI 13 / systolic 9 / HbA1c 4 / LDL 1 patients).

| PR | Branch | Scope | Acceptance |
|---|---|---|---|
| 1 | `docs/adr-034-doug-wave` | ADR-034, spec+plan, D17 answer-log supersession, CLAUDE.md focus, JOURNAL; file issues (shim; Codify/upstream; #292 comment) | ADR-034 merged before any mysql2 code |
| 2 | `feat/wcdb-shim-scaffold` | `wcdb-fhir-shim/` package (node:http + mysql2), `/fhir/metadata`, paged `/fhir/Patient` w/ same-origin next links, compose `wcdb` profile (wcdb + shim :8085), Dockerfile, stubbed-query unit tests | compose up; metadata 200; paging walks 56; tests green w/o Docker |
| 3 | `feat/wcdb-shim-clinical` | `fhir-mapping.ts` (ported from `webchart-devdb-export.ts`), Observation/Procedure endpoints, empty Condition/Immunization/Encounter | `hapi-live.test.ts` green at `WORKWELL_WEBCHART_LIVE_TEST_BASE_URL=http://localhost:8085`; `evaluate:webchart-live` matches fixture path; in-app live-tenant renders |
| 4 | `feat/codegen-generate-sql` | `generate-sql.ts` (pure, beside `generate-cql.ts`), `pnpm generate:sql` → committed `wcdb-fhir-shim/sql/*.sql`, snapshot + freshness tests; final demo-measure gate from LOINC counts | deterministic regeneration; snapshots reviewed |
| 5 | `feat/shim-compliance-api` | `compliance.ts` executes committed SQL w/ bound params; `/compliance/{patientId}/{measureId}` + `/compliance/{measureId}/cohort` | cohort curl sane counts ≤ 56 vs live wcdb |
| 6 | `test/sql-cql-parity` | `parity.live.test.ts` (self-skip on `WCDB_SHIM_PARITY_BASE_URL`): SQL vs CQL-over-shim-FHIR per patient; `WORKWELL_WEBCHART_ENROLLMENT_JSON` doc-drift fix; dated parity note | parity green for demo measures across 56; **ADR-025 gate** |
| 7 | `feat/codify-demo-docs` | Codify CodeLookup probe (timeboxed 3h; integrate or document+file ask), `docs/DEMO_2026-07-23.md` (script + fallback + owner outreach), JOURNAL/CLAUDE.md refresh | demo script executes verbatim from clean state; Codify outcome explicit |

> **Ordering vs ADR-025 (Codex P1):** PR-5 exposing the compliance API before PR-6's parity gate is
> stack-internal sequencing, not a serving window — the wave merges as ONE reviewed stack after the
> PR-6 gate ran green, the shim never deploys anywhere, and (belt-and-braces) the compliance
> endpoints themselves fail closed: any measure not on the shim's `PARITY_CERTIFIED` allowlist is
> refused with 409, so no artifact is ever served ahead of its green parity run.

Cut order under time pressure: Codify integration → hba1c stretch → cohort endpoint → live-LLM
garnish. Never cut: PR-2/3, PR-6, demo script. Demo fallback: committed fixtures + `pnpm load:hapi`
→ HAPI :8081 (identical dashboards).
